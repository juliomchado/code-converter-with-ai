# CodeConverter - Client-Side Only Application
# 
# This application does NOT use environment variables for API keys.
# All API keys are entered by users through the browser interface.
# 
# This file exists only for documentation purposes.
# No environment variables are needed for deployment.
#
# Users will enter their API keys directly in the application:
# 1. Open the app
# 2. Click "API Configuration" → "Configure" 
# 3. Select provider and enter their own API key
# 4. API keys are stored locally in browser only
#
# Recommended free option for users:
# Google Gemini: https://aistudio.google.com (15 requests/minute free)